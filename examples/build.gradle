extensions.xrunDir = file("$buildDir/xrun")

file('.').eachDir { exampleDir ->
    task("run_${exampleDir.name}", type: RunExampleTask) {
        exampleName exampleDir.name
        dependsOn project(':').genFullArgsFile
    }
}
