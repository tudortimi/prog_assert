extensions.xrunDir = file("$buildDir/xrun")

projectDir.eachDir { dir ->
    if (dir != buildDir) {
        task("run_${dir.name}", type: RunExampleTask) {
            exampleDir dir
            dependsOn project(':').genFullXrunArgsFile
        }
    }
}
